apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend
  labels:
    app: frontend
    stack: demo-apps
spec:
  selector:
    matchLabels:
      app: frontend
      stack: demo-apps
  endpoints:
    - port: http
      path: /metrics
      interval: 15s